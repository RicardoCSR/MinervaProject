#include <Arduino.h>

void listFiles(void);
// Carrega lista de arquivos armazenados no SPIFFS
void listDir(void);
// Apresenta lista de arquivos armazenados no SPIFFS
void smoothText(void);
// Utiliza arquivos armazenados no SPIFFS ou SDCard dos textos com Anti-Alising

